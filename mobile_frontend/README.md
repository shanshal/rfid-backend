# RFID mobile frontend

Mobile-first React app for scanner/device/instrument registration and status monitoring.

## Run locally

```bash
cd mobile_frontend
npm install
npm run dev
```

Optional env vars:

- `VITE_API_URL` default `http://localhost:8000`
- `VITE_ACTOR` default `mobile-admin`
